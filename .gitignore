bin/
obj/
logs/
*.log
*.user
.vs/
.vscode/
.idea/
.DS_Store
Thumbs.db

# CI artifacts (generated, uploaded from the workflow — never committed)
sbom/

# Secrets & local config — never commit these. Use the committed *.template as a starting point.
appsettings.Production.json
appsettings.Development.json
appsettings.Local.json
appsettings.*.Local.json
.env
.env.*
*.pfx
*.key
secrets.json

# Session handoff scratch note (kept on disk for cross-session context, not versioned)
docs/HANDOFF.md

# Vendor-internal operations (quotes, minting runbook, issuance ledger) — never published.
docs/internal/
docs/GTM_STRATEGY.md
